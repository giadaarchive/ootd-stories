# Secrets & credentials
.env
.substack_cookies.json

# Runtime state (machine-specific, not portable)
.substack_schedule_state.json
wardrobe_last15.json

# Python
__pycache__/
*.pyc
*.pyo

# Logs
*.log

# macOS
.DS_Store

# Images (hosted separately via GitHub API, not committed as git objects)
collection-images/

# Andromeda runtime (cache is ephemeral; data/ is committed as seeded graph)
house_codes/cache/

# Next.js
frontend/node_modules/
frontend/.next/
frontend/.env.local
